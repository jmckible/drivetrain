[ids]
*

[main]
# Remap SUPER-W to CTRL-W (for closing browser tabs).
# Use explicit M-<key> in [main] (not a [meta:M] layer) so the remap fires
# only on bare SUPER+key — combos like SUPER+CTRL+R pass through to Hyprland.
M-w = C-w
