# Helium Browser flags (Desktop)
# Scale factor optimized for desktop monitors
--force-device-scale-factor=0.75

# Wayland support
--ozone-platform=wayland
--ozone-platform-hint=wayland

# Features
--enable-features=TouchpadOverscrollHistoryNavigation

# Load Omarchy extension
--load-extension=~/.local/share/omarchy/default/chromium/extensions/copy-url

# Chromium crash workaround for Wayland color management on Hyprland
# See https://github.com/hyprwm/Hyprland/issues/11957
--disable-features=WaylandWpColorManagerV1

# Note: Advanced GPU flags (Vulkan, hardware video encode, WebGPU) tested
# but caused blank window on NVIDIA GTX 1650 + Wayland + Hyprland
# Keeping it simple - hardware acceleration already works well without them
