general {
    after_sleep_cmd = hyprctl dispatch dpms on && sudo systemctl restart bluetooth && omarchy-theme-bg-auto  # turn on display, reconnect bluetooth, update wallpaper
}

listener {
    timeout = 330                                            # 5.5min
    on-timeout = brightnessctl -sd '*::kbd_backlight' set 0  # save state and turn off keyboard backlight
    on-resume = brightnessctl -rd '*::kbd_backlight'         # restore keyboard backlight
}

listener {
    timeout = 700                                            # ~12min
    on-timeout = hyprctl dispatch dpms off                   # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r && sudo systemctl restart bluetooth # screen on, reconnect bluetooth mouse
}
