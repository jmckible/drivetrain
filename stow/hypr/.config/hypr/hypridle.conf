general {
    lock_cmd = omarchy-lock-screen                         # lock screen and 1password
    before_sleep_cmd = loginctl lock-session               # lock before suspend. slight delay for fprintd
    after_sleep_cmd = hyprctl dispatch dpms on && omarchy-theme-bg-auto # turn on display and update wallpaper
    on_unlock_cmd = omarchy-theme-bg-auto                  # update wallpaper when unlocking screen
    inhibit_sleep = 3                                      # wait until screen is locked
}

listener {
    timeout = 600                                             # 5min
    on-timeout = pidof hyprlock || omarchy-launch-screensaver # start screensaver (if we haven't locked already)
}

listener {
    timeout = 86400                    # 24 hours
    on-timeout = loginctl lock-session # lock screen when timeout has passed
}

listener {
    timeout = 700                                            # 5.5min
    on-timeout = hyprctl dispatch dpms off                   # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r # screen on when activity is detected
}
